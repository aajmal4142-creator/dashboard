# ClearESG — AI deferred (§16)

Do **not** build in the current launch pass:

- Copilot / assistant of any kind
- ML anomaly detection beyond deterministic rules in `src/lib/governance/anomalies.ts`
- Any model, embedding, or inference provider keys/stubs

Start only after a real pilot completes buy → onboard → collect → publish → share → audit-trace.

Permitted preparation only: keep glossary/answers structured for SEO/AEO.
