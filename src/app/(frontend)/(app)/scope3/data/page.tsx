import { ActivityRecordsClient } from "./ActivityRecordsClient";

export const metadata = {
  title: "Activity records | ClearESG",
};

export default function Scope3DataPage() {
  return <ActivityRecordsClient />;
}
